--- a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
+++ b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
@@ -791,7 +791,10 @@
 }
 // Normalize tool call IDs to match Anthropic's required pattern and length
 function normalizeToolCallId(id) {
-    return id.replace(/[^a-zA-Z0-9_-]/g, "_").slice(0, 64);
+    if (!id)
+        return "tool_call_0";
+    const normalized = id.replace(/[^a-zA-Z0-9_-]/g, "_").slice(0, 64);
+    return normalized || "tool_call_0";
 }
 function convertToolResult(msg, isOAuthToken, deferredToolNames, loadedToolNames, normalizeToolName) {
     const references = [];
@@ -810,7 +813,7 @@
     return {
         toolResult: {
             type: "tool_result",
-            tool_use_id: msg.toolCallId,
+            tool_use_id: normalizeToolCallId(msg.toolCallId),
             content: references.length > 0 ? references : convertedContent,
             is_error: msg.isError,
         },
@@ -918,7 +921,7 @@
                 else if (block.type === "toolCall") {
                     blocks.push({
                         type: "tool_use",
-                        id: block.id,
+                        id: normalizeToolCallId(block.id),
                         name: isOAuthToken ? toClaudeCodeName(block.name) : block.name,
                         input: block.arguments ?? {},
                     });
